`import processQuery from '../../../utils/process-query'`
`import { module, test } from 'qunit'`

module 'Unit | Utility | process query'

# Replace this with your real tests.
test 'it works', (assert) ->
  result = processQuery()
  assert.ok result
